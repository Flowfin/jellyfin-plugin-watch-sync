# Evidence only

This file exists so that this branch's diff against master touches Markdown and
nothing else. It is not intended to land, and the pull request carrying it is
closed rather than merged.

What it is evidence for is in #134: whether the build and the suite still report
their check runs when a change touches only Markdown, and whether those checks
still refuse a change that breaks the suite.
