# Declared departures from the headless rule.
#
# Three fields per entry, separated by " :: ":
#   path relative to the repository root :: vocabulary id :: the reason
#
# An entry whose file is not in the scanned set, or whose file no longer contains a
# hit for that id, fails the guard as dangling. An exception is a debt with the thing
# that retires it written next to it, not a permanent hole, so it is removed by the
# change that removes the call rather than left to rot.
#
# Blank lines and lines beginning with # are ignored.
#
# One entry today, and it reads no time. The fact that holds the composition root to
# handing out the runtime's own clock has to name that clock to compare identity with
# what was resolved, which is the whole of what it does with it: no moment is read, no
# outcome depends on when the suite ran, and the rule that keeps the machine clock out
# of the suite is the rule that fact exists to close from the plugin's side. It is
# retired by the day the composition root hands out something else, which is the day
# the invariant register's own departure for that file goes dangling too.

Jellyfin.Plugin.WatchSync.Tests/ClockEntryTests.cs :: clock-time-provider-system :: the fact compares the clock the composition root hands out against the runtime's own by identity and reads no time from either; it is the suite's side of the one declared departure in Invariants/exceptions.txt
