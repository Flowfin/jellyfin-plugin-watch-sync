# A slice of the publish route whose pin was raised while the merge gate's was left where it
# was, which is what a dependency bump looks like when it is made in the file somebody had
# open. Both routes still write an inventory and both runs are green; the two describe one
# graph with two versions of one tool.

  build:
    name: Build package
    steps:
      - name: Build the plugin package
        id: jprm
        uses: oddstr13/jellyfin-plugin-repository-manager@9497a0a499416cc572ed2e07a391d9f943a37b4d # v1.1.1
        with:
          dotnet-target: "net9.0"

      - name: Write the component inventory beside the package
        id: inventory
        env:
          ARTIFACT: ${{ steps.jprm.outputs.artifact }}
        run: |
          set -euo pipefail
          directory="$(dirname "${ARTIFACT}")"
          dotnet tool install --global CycloneDX --version 7.0.0
          export PATH="${HOME}/.dotnet/tools:${PATH}"
          dotnet-CycloneDX "Jellyfin.Plugin.WatchSync/Jellyfin.Plugin.WatchSync.csproj" \
            --output-format Json \
            --output "${directory}" \
            --filename components.cdx.json \
            --set-nuget-purl \
            --exclude-test-projects \
            --disable-package-restore
          echo "inventory=${directory}/components.cdx.json" >> "${GITHUB_OUTPUT}"
