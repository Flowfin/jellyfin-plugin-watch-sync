Issue: #36
Existing-Data: unchanged

A first exchange now writes down what it discarded. One record per moved field the two
servers disagreed about, holding both readings, which rule of the conflict table decided,
and which side's reading did not survive. An operator asking why an episode is marked
watched, or why a position moved, can be answered from what the run wrote rather than from
a hypothesis.

A field the two servers already agreed about leaves nothing, because there was no
disagreement to resolve. An item the run could not decide leaves nothing either: both sides
keep exactly what they held, and what it carries instead is the reason it was left standing.

Nothing calls the run yet. There is no scheduled sweep and no apply path, so no server
behaves differently for this and no watch state moves that did not move before.
