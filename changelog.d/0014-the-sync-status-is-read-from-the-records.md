Issue: #62
Existing-Data: unchanged

The sync status of one pairing for one person can be read, and the unmatched items of that
pairing and person can be exported. Every number is read from the record the code uses and
never counted for display: the moment of the last exchange is the point the peer last
confirmed, the unmatched count and its top reasons are the unmatched record, the conflicts
are the conflict record, and whether a run was stopped by the cap is the plan the stop
recorded. A record that could not be read is shown as unreadable rather than as zero, and a
stopped run is the first thing the status says.

The configuration page has a section that asks for the pairing and the person and shows the
status, with a stopped run above the rest, and shows the export for copying. Both calls need
an administrator account.

What the last exchange did, whether the peer is unreachable and the last refusal have no
record yet and are not shown. Nothing produces any of the records this reads, so on a server
today every section answers that it is absent, and no watch state moves that did not move
before.
